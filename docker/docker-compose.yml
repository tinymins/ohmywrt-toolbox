name: ohmywrt-toolbox

services:
  # PostgreSQL 数据库
  db:
    image: postgres:18
    container_name: ohmywrt-toolbox-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-ohmywrt_toolbox_db}
    volumes:
      - ./${DATA_LOCAL_PATH:-data}/postgres:/var/lib/postgresql
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 后端服务（Rust 二进制 + 内嵌前端静态文件）
  server:
    image: ohmywrt-toolbox-server:latest
    container_name: ohmywrt-toolbox-server
    build:
      context: ..
      dockerfile: packages/server/Dockerfile
    ports:
      - "${WEB_PORT:-8080}:5678"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-ohmywrt_toolbox_db}
      RUST_LOG: ${RUST_LOG:-info}
      DATA_LOCAL_PATH: ./${DATA_LOCAL_PATH:-data}
      PUBLIC_SERVER_URL: ${PUBLIC_SERVER_URL:-}
    volumes:
      - ./${DATA_LOCAL_PATH:-data}/server:/app/data
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
